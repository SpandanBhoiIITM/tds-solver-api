echo "# TDS Solver API" > README.md
echo "This repository contains an API for solving TDS-related problems using an LLM-based approach." >> README.md
git add README.md
git commit -m "Added README"
git push origin main
